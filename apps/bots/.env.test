# E2E Test Environment Variables
NODE_ENV=test

# Test identifiers (mock values for E2E)
POOL_SLOT_UUID=e2e-test-slot

# Backend API (mock values, not used in E2E tests)
MILO_URL=http://localhost:3333
MILO_AUTH_TOKEN=e2e-test-token

# Debug
HEARTBEAT_DEBUG=false
