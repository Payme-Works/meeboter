version: 0.1

projects:
  # Active ECS Fargate infrastructure for meeting bots
  - path: terraform/bots
    name: meeboter-bots
    dependency_paths:
      - terraform/bots/**/*.tf
      - terraform/bots/terraform.tfvars
    usage_file: terraform/infracost-usage.yml
    terraform_var_files:
      - terraform.tfvars
